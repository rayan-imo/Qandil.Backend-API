namespace Qandil.Core.Enums
{
    public enum AnswerFrequency
    {
        Never = 0,   // أبداً
        Rarely = 1,  // أحياناً
        Often = 2,   // غالباً
        Always = 3   // دائماً
    }
}
