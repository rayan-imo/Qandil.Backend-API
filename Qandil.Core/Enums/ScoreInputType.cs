namespace Qandil.Core.Enums
{
    public enum ScoreInputType
    {
        Frequency = 0,   // دائماً/غالباً/أحياناً/أبداً → AnswerFrequency (0-3)
        RawNumber = 1    // رقم صريح مباشر (متلاً 0-2)
    }
}
